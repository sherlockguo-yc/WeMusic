# 下一首播放

## 用户场景

用户正在听歌时，浏览搜索结果、歌单、发现页，看到想听的歌但不希望打断当前播放。点击「下一首播放」将该歌曲插入到当前歌曲之后，当前歌曲播完后自动切到该歌曲。

**典型流程**：
1. 用户正在听「晴天」→ 在搜索页搜「七里香」→ 右键 → 下一首播放 → toast 提示 → 继续浏览
2. 当前歌曲播完后自动播放「七里香」

## 抽屉入口与最近播放（2026-08-12 更新）

### 入口调整

- **侧边栏「最近播放」导航项已移除**：不再占用左侧菜单栏位置
- **右下角按钮**：文字「队列 / 历史」（hover 提示「播放队列 / 最近播放」），点击打开队列抽屉

### 抽屉结构（播放队列 + 最近播放合并抽屉 `#queueDrawer`）

- 两个 tab：「播放队列」（`tabQueue`）与「最近播放」（`tabHistory`）
- 打开抽屉时 tab 选择逻辑（`pickInitialTab`，优先级从高到低）：
  1. **记忆优先**：用户主动切换过 tab → 使用 localStorage `wemusic_qd_tab` 记录的选择
  2. **智能默认**：无记忆且队列为空 → 自动显示「最近播放」（空队列场景直接展示可播放内容，解决「队列」入口不直白、最近播放不可发现的问题）
  3. 无记忆且有队列 → 显示「播放队列」
- 用户点击 tab 时写入记忆（`localStorage.setItem(QD_TAB_KEY, ...)`）

### 渲染一致性

- 队列变化（`enqueueNext` / `removeFromQueue`）后统一走 `renderActiveTab()`，而非无条件 `renderQueue()`——`qdList` 是队列与最近播放共用的渲染容器，无条件渲染队列会把「最近播放」tab 下的列表覆盖成队列内容，造成 tab 高亮与内容不一致

### 本次涉及文件

| 文件 | 改动 |
|------|------|
| `public/index.html` | 删除 `navHistory` 菜单项；队列按钮文字改「队列 / 历史」 |
| `src/queue.js` | `pickInitialTab()` 智能默认 + tab 记忆；`enqueueNext`/`removeFromQueue` 改用 `renderActiveTab()`；删除 `navHistory` 点击监听与外部关闭排除 |
| `src/playlist-ui.js` | `setActiveNav()` 数组移除 `navHistory` |

## 设计决策（2026-07-10 讨论）

| 决策 | 选择 |
|------|------|
| 菜单项数量 | 1 个：「下一首播放」 |
| 行为 | 插入到当前歌曲之后（`splice(queueIndex+1, 0, song)`），不是队列末尾 |
| 队列空时 | 仍显示「下一首播放」，行为 = 作为 queue[0]，不自动播放 |
| 替换队列 | 队列有待播歌时 toast 提醒，不弹确认框 |
| 业界参考 | 简化版（参考 Spotify 单选项模式 + Apple Music 的插队行为） |

## 现状分析

### 当前队列行为

- 点击歌曲行 / 右键「播放」→ `playFromList(songs, index)` **替换整个队列**（`state.queue = songs`）
- 没有「追加到现有队列」的能力
- 右键菜单项：添加到歌单 / 分享 / 复制 Bilibili 链接 / 在 Bilibili 搜索此歌 / 从歌单移除

### 数据模型（现有，无需改动）

```js
// state.js
state.queue = [];       // 播放队列数组
state.queueIndex = -1;  // 当前播放索引
```

## 功能设计

### 入口：右键菜单（所有歌曲行通用）

```
┌──────────────────────────┐
│ 下一首播放               │  ← 新增
├──────────────────────────┤
│ 添加到歌单               │
│ 分享                     │
│ 复制 Bilibili 链接       │
│ 在 Bilibili 搜索此歌     │
├──────────────────────────┤
│ 从歌单移除               │  ← 仅在歌单内时显示
└──────────────────────────┘
```

### 视觉反馈

| 触发时机 | 反馈方式 |
|----------|----------|
| 正在播放，添加成功 | Toast：「「歌名」将下一首播放」（2s 自动消失） |
| 队列空，添加成功 | Toast：「「歌名」已添加到播放队列」 |
| 队列有待播歌，执行替换播放 | Toast：「已替换播放队列」 |
| 队列提醒 | 添加后 0.5s 内队列按钮短暂脉冲动画一次（`transform: scale(1.1)`） |

### 行为细节

- **队列为空且无正在播放时**：添加到 queue[0]，不自动播放
- **不去重**：用户可以同一首歌添加多次（有意为之，比如想循环听某首歌 N 遍）
- **跨视图**：在搜索结果页 / 歌单页 / 发现页 / 专辑页均可触发
- **持久化**：添加后自动 `saveSession()`，刷新页面队列不丢失
- **右键菜单选中后立即关闭菜单**
- **`playFromList` 替换队列时**：如果队列中有待播歌（用户手动加过「下一首播放」），toast 提醒「已替换播放队列」

## 状态机

```
                    ┌─────────────────────┐
                    │   queueIndex >= 0    │
                    │   (正在播放中)        │
                    └────────┬────────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
       下一首播放                       其他操作
       (splice after                  (不变)
        queueIndex)
              │
              ▼
     ┌────────────────────┐
     │ toast: "将下一首播放" │
     │ saveSession          │
     │ renderActiveTab      │
     └────────────────────┘

                    ┌─────────────────────┐
                    │   queueIndex < 0     │
                    │   (队列空/未播放)     │
                    └────────┬────────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
       下一首播放                       其他操作
       (queue[0],                   (不变)
        不自动播放)
              │
              ▼
     ┌──────────────────────┐
     │ toast: "已添加到播放队列" │
     │ saveSession           │
     │ renderActiveTab       │
     └──────────────────────┘
```

## 实现要点

### 核心函数（新增 `src/queue.js`）

```js
// 插入到下一首
function enqueueNext(song) {
  if (state.queueIndex >= 0) {
    state.queue.splice(state.queueIndex + 1, 0, song);
    toast(`「${song.name}」将下一首播放`);
  } else {
    state.queue.push(song);
    toast(`「${song.name}」已添加到播放队列`);
  }
  saveSession();
  renderActiveTab();
  pulseQueueBtn();
}
```

### 修改点

| 文件 | 改动 |
|------|------|
| `src/queue.js` | 新增 `enqueueNext()`、`pulseQueueBtn()` |
| `src/ui.js` | `openSongMenu()` 右键菜单新增「下一首播放」 |
| `src/player.js` | `playFromList()` 中有待播歌时 toast 提醒 |
| `public/css/style.css` | 队列按钮脉冲动画 |
| `public/index.html` | 无需改动 |

### 不需要改动的

- `state.js` — 数据模型不变
- `server/` — 无需服务端改动

## 验收标准

1. 正在播放时，右键任意歌曲行 → 出现「下一首播放」
2. 点击「下一首播放」→ toast 提示 "「歌名」将下一首播放" + 当前歌曲播完立即切到该歌
3. 连续「下一首播放」3 首不同歌 → 按添加顺序依次播放
4. 队列为空时右键 → 「下一首播放」可见，点击后 toast "已添加到播放队列"
5. 刷新页面 → 队列中通过「下一首播放」加的歌仍存在
6. 从搜索页 / 歌单页 / 发现页 / 专辑页均可触发
7. 队列中有待播歌时点击播放新歌 → toast "已替换播放队列"
