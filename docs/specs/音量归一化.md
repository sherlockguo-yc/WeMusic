# 音量归一化

## 概述

不同 B 站视频源的音量差异巨大（UP 主录制时音量各不相同），用户连续播放多首歌时需要频繁手动调整音量。目标：自动标准化响度，让不同源之间的音量大致一致。

**状态**：✅ 已实现（2026-07-10），**2026-07-13 架构重写后归一化变为全场景生效**（不再仅后台）

> 相关规格文档（新架构）：[后台播放.md](../../specs/后台播放.md)、[播放进度与音量控制.md](../../specs/播放进度与音量控制.md)
> 相关需求：「[音量控制统一](./音量控制统一.md)」— 该问题已由 2026-07-13 架构改造从根本上解决（bgAudio 单一音频源消除了前后台音量差异）。本文档保留归一化的技术实现细节（EBU R128、gain 计算、DB 建表等），这些在新架构下仍然准确。

---

## 技术概念

### RMS（均方根）= 音频的平均响度

音频是一串随时间变化的数字（采样点），每个数字代表那一瞬间声波的振幅。RMS 把所有采样点的振幅平方、求平均、再开平方根，得到一个代表「整体响度」的数字。

```
RMS = 0.02  → 很轻，听不清
RMS = 0.08  → 正常
RMS = 0.30  → 震耳朵
```

### Gain（增益）= 音量倍率

Gain 是一个乘数，放在「音频源」和「耳朵」之间：

```
音频源 → [Gain 倍率] → 你听到的声音
```

- `gain = 1.0`：不改变
- `gain = 0.3`：压到原来 30%（对付太吵的源）
- `gain = 3.0`：放大 3 倍（对付太小声的源）

例：歌曲 A 的 RMS 是 0.30（太吵），歌曲 B 的 RMS 是 0.05（太小）。以 0.08 为基准：

- 歌曲 A：`gain = 0.08 / 0.30 = 0.27`（压低）
- 歌曲 B：`gain = 0.08 / 0.05 = 1.60`（放大）

### EBU R128（行业标准响度）

简单 RMS 只反映平均振幅，和人耳实际听感不完全一致。EBU R128 是广播行业的响度标准，模拟人耳对不同频率的敏感度，比简单 RMS 更准确。ffprobe 内置 EBU R128 分析能力。

---

## 音量层级模型

```
B站源音频 → [Gain 归一化] → [OS 系统音量] → 耳朵
   ↑              ↑                 ↑
 原始响度      自动调节           用户手动调
```

- **Gain 归一化**：自动为每个视频源计算一个倍率，让不同源的响度对齐。**存储于服务端 DB**，跨用户共享
- **全场景生效**（2026-07-13 架构改造后）：bgAudio 是唯一常规音频源（无论前台/后台/锁屏），归一化通过 AudioContext GainNode 始终生效。仅展开视频看画面时归一化不生效（此时声音由 B 站 iframe 独立输出，WeMusic 无法在 iframe 上叠加 GainNode）。详见 `docs/specs/后台播放.md`

---

## 详细实现流程

### 一句话概括

播放一首歌时，服务端在后台偷偷下载完整音频、用 ffprobe 测出它的响度、算出一个「增益倍率」存起来。下次播放同一首歌时，浏览器直接用这个倍率把音量调准。

---

### 阶段 0：APP 启动时（只执行一次）

```
1. 用户打开 WeMusic 网页
   ↓
2. initPlayer() 执行：
   ↓
3. _initAudioCtx() 被调用：
   a) 创建 new AudioContext() — 浏览器的音频控制中心
   b) 调用 audioCtx.createMediaElementSource(bgAudio)
      → 把 <audio id="audio"> 这个元素「接」到 AudioContext 的管线里
      → ⚠️ 这一步只能做一次！以后换歌只是改 bgAudio.src，管线不用重建
   c) 创建 audioCtx.createGain() — 一个「音量阀门」
   d) 连接管线：
      bgAudio → GainNode → 扬声器
      （音频从 <audio> 流出来 → 经过阀门 → 到你的耳朵）
   e) 注册点击/按键事件：一旦用户点了任何地方，就调用 audioCtx.resume()
      → 因为浏览器出于安全考虑，AudioContext 刚创建时是「暂停」状态
      → 必须有用户交互（点击/按键）才能激活
   ↓
4. _applyNormVol() 执行：
   阀门开度 = safeGain（归一化增益）
   （不再混入用户手动音量乘数）
```

---

### 阶段 1：用户点播放《晴天》

```
5. playCurrent() 被调用
   ↓
6. 从播放队列取到这首歌：{ name: "晴天", singer: "周杰伦", ... }
   ↓
7. 检查这首歌有没有 B站的视频 ID（bvid）？
   ├─ 没有 → 调 POST /api/play/resolve { name: "晴天", singer: "周杰伦" }
   │          → 服务端从 B站搜索匹配合适的视频
   │          → 返回 { best: { bvid: "BV1xx...", title: "晴天-周杰伦 MV" } }
   │          → 存到 song.bvid = "BV1xx..."
   └─ 有 → 跳过
   ↓
8. 调用 startVideo("BV1xx...", "晴天-周杰伦 MV", 269秒)
```

---

### 阶段 2：startVideo — 加载音频源并触发 gain 查询

> **2026-07-13 架构已重构**：startVideo 的流程已大幅简化，详情见 `docs/specs/后台播放.md`。
> 
> 简化后的流程：
> - `mode === 'bg'`（默认）：`_loadAndPlayBgTrack(bvid)` → 请求 direct-url → 设 bgAudio.src → play
> - `mode === 'iframe'`（展开视频时）：`mountVideoAt(bvid, title, 0)` + 静默预载 bgAudio
> 
> gain 查询（`_fetchGain`）在 `_loadAndPlayBgTrack` / `_loadBgTrack` 中异步触发，不阻塞播放。
> 归一化增益始终作用于 bgAudio 的 GainNode（全场景生效，不再区分前后台）。

```


---

### 阶段 3：gain 查询与音频加载（同时进行）

> **2026-07-13 架构已重构**：`_bgPreload` 函数已删除。新架构中 `_fetchGain(bvid)` 与音频加载并行，不再有静音预缓冲步骤。
> 
> 简化后的流程：
> - `_getPlayableUrl(bvid)` 优先取 CDN 直链，失败才走代理 `/api/play/stream`
> - `_fetchGain(bvid)` 异步 fire-and-forget，不阻塞播放
> - 以下服务端 gain 查询 和 stream 端点的逻辑不变

```
10. _fetchGain("BV1xx...") 异步触发：

     11. fetch('/api/play/gain?bvid=BV1xx...')
         ↓
     12. 服务端收到请求：
         a) 从 bvid 解析 cid：调 B站 API 拿到视频第一P的 cid = 123
         b) 查 SQLite：
            SELECT gain_mult, status FROM gain_cache WHERE bvid='BV1xx...' AND cid=123
         c) 结果：
            ├─ 第一次播放 → 表里为空 → 返回 { gain: null, status: null }
            └─ 第二次播放 → 表里有 { gain_mult: 0.47 } → 返回 { gain: 0.47, status: "complete" }
         ↓
     13. 前端收到响应：
         ├─ gain=null → 什么都不做，_normGain 保持 1.0
         └─ gain=0.47 → _normGain = 0.47 → 立刻调 _applyNormVol()：
                        阀门开度 = safeGain = min(0.47, 8.0) = 0.47

    10B. 设置 bgAudio 的音频源（与 10A 并行进行）
         ↓
     14. _getPlayableUrl(bvid) → 优先返回 CDN 直链，失败走 /api/play/stream 代理
     15. bgAudio.volume = 1.0（永远 1.0，音量由 GainNode 阀门控制）
     16. bgAudio.load()
         → 浏览器直接向 B站 CDN 发起 HTTP GET 请求（直连时），或向服务端发起请求（代理时）

### 阶段 4：音频加载（直连优先，代理兜底 + 触发增益分析）

> 新架构：前端优先直连 B站 CDN，失败才走服务端 proxy。无论哪种路径，服务端都会在 stream 端点和 direct-url 端点中触发增益分析。

19. 音频加载（两条路径之一）：
    a) 直连 CDN：《/api/play/direct-url》返回 { url, ... } → bgAudio.src = cdnUrl → 浏览器直接下载
    b) 代理兜底：GET /api/play/stream?bvid=...  → 服务端从 B站 CDN 拉取再转发
```
22. 检查：这个视频的 gain 分析过了吗？
    SELECT status FROM gain_cache WHERE bvid='BV1xx...' AND cid=123

    ├─ status='complete' → 已有 gain，跳过分析
    ├─ status='pending'  → 正在分析中，跳过（不重复触发）
    └─ 无记录 或 status='failed' → 需要分析！
        ↓
    23. 往 DB 写一条 pending 记录：
        INSERT INTO gain_cache (bvid, cid, gain_mult, status)
        VALUES ('BV1xx...', 123, 1.0, 'pending')
        ↓
    24. enqueueAnalysis('BV1xx...', 123, cdnUrl)：
        → 放入串行队列，不阻塞。分析使用独立的 B站 CDN 连接（与客户端播放无关）

> 增益分析的下游流程（下载完整文件→ffmpeg→calcGain→写DB）完全不变，详见阶段 5。
```

---

### 阶段 5：后台分析（在串行队列里慢慢跑）

```
26. 轮到 BV1xx 的分析任务了，runAnalysis() 执行：

    26a. 下载完整音频文件：
         fetch(cdnUrl) // 注意：这次不带 Range 头，要完整的文件！
         → 从 B站 CDN 独立下载
         → 和你的播放用的是两条不同的 HTTP 连接
         → 即使你切歌了、关网页了，这个下载照样跑完
         → 写入 /tmp/wemusic_gain_BV1xx_123_1752110400000.mp4
         ↓
    26b. 文件下载完成，比如 3.2MB
         ↓
    26c. 运行 ffmpeg 分析响度：
         ffmpeg -i /tmp/wemusic_gain_BV1xx_123_xxx.mp4 \
           -af ebur128=video=0 \
           -f null -
         
         → stderr 输出包含 Summary 段
         ↓
    26d. 解析 Summary 中的 Integrated loudness 的 I: 值
         → 例：I:  -16.5 LUFS
         ↓
    26e. 计算增益倍率：
         目标响度 = -18 LUFS（消费端标准）
         实际响度 = -16.5 LUFS
         
         raw_gain = 10^((-18 - (-16.5)) / 20)
                 = 10^(1.5 / 20)
                 = 1.189
         
         clamp(1.189, 0.05, 10.0) = 1.189（在范围内）
         ↓
    26f. 写入 DB：
         UPDATE gain_cache
         SET gain_lufs = -16.5,
             gain_mult = 1.189,
             status = 'complete',
             updated_at = 1752110420000
         WHERE bvid = 'BV1xx...' AND cid = 123
         ↓
    26g. 删除临时文件：
         rm /tmp/wemusic_gain_BV1xx_123_xxx.mp4
         ↓
    26h. 打印日志：
         [gain] done bvid=BV1xx... cid=123 lufs=-16.5 gain=1.189
```

---

### 阶段 6：第二次播放同一首歌（已有 gain 缓存）

```
27. 你又点了《晴天》（或自动切到了这首歌）
    ↓
28. _bgPreload("BV1xx...") → _fetchGain("BV1xx...")
    ↓
29. GET /api/play/gain?bvid=BV1xx...
    → 服务端查 DB → 返回 { gain: 1.189, status: "complete" }
    ↓
30. 前端收到：
    _normGain = 1.189
    ↓
31. _applyNormVol()：
    阀门开度 = min(1.189, 8.0) = 1.189
    _gainNode.gain.value = 1.189
    ↓
32. 音频播放：
    B站源声音 → 经过阀门 → 和其他歌一样响了 ✅
```

---

### 阶段 7：用户切换归一化开关

```
33. 你打开设置面板，关了「音量标准化」：
    localStorage.setItem('wemusic_volume_normalize', '0')
    ↓
34. 下次 _applyNormVol() 检测到开关关闭：
    阀门开度 = 1.0（原始音量）
    → 你听到了原始音量（没有归一化）

35. 你再打开开关：
    localStorage.setItem('wemusic_volume_normalize', '1')
    ↓
36. 阀门开度又变回 safeGain
```

---

## 边界情况

### 触发时机

- 用户在任意场景播放一首歌（直连 CDN 或服务端代理），`_fetchGain(bvid)` 异步查询 gain 缓存
- 服务端 `/api/play/direct-url` 和 `/api/play/stream` 端点均会在首次请求时触发后台异步增益分析（逻辑一致）

**结果：无论用户使用直连还是代理，只要播放一个视频，就会触发 gain 分析。纯前台用户也能贡献 gain 数据。**

### 用户操作对分析的影响

| 操作 | 影响 |
|------|------|
| 前后台切换 | 不影响。服务端从 B站 CDN 的下载独立于客户端连接 |
| 暂停 | 不影响。bgAudio 暂停是客户端行为，不中断 HTTP 连接 |
| 调整进度（seek） | 不影响。gain 看完整文件响度，和播放位置无关 |
| 切歌 | 不影响已触发的分析。新歌的 stream 请求独立处理 |

### 重复与并发

| 场景 | 处理 |
|------|------|
| 同一首歌播第二遍 | 跳过（DB 已有 status='complete'） |
| 两个用户同时播同一首 | DB pending 标记去重 |
| N 首不同歌同时触发 | 串行队列，一次分析一个 |

### 数据准确性

| 问题 | 答案 |
|------|------|
| DASH 音频 vs H5 durl 回退，gain 还准吗？ | 准。音频轨道内容相同，不同码率/容器不影响 PCM 响度 |
| Gain 基准值设多少？ | -18 LUFS（消费端标准，Apple/YouTube 区间） |
| 短音频？ | 时长 < 10 秒跳过 |

---

## 实现细节

### DB 建表

```sql
CREATE TABLE IF NOT EXISTS gain_cache (
  bvid        TEXT NOT NULL,
  cid         INTEGER NOT NULL DEFAULT 0,
  gain_lufs   REAL,
  gain_mult   REAL NOT NULL DEFAULT 1.0,
  title       TEXT DEFAULT '',
  status      TEXT DEFAULT 'pending',  -- pending / complete / failed
  created_at  INTEGER NOT NULL,
  updated_at  INTEGER NOT NULL,
  PRIMARY KEY (bvid, cid)
);
```

### 分析流程（服务端）

1. stream 端点检查 `gain_cache WHERE bvid=? AND cid=?`
2. 无记录或 failed → INSERT pending + 入队串行分析
3. 分析线程：fetch B站 CDN（独立连接，无 Range）→ write `/tmp/wemusic_gain_xxx.mp4` → ffmpeg → calcGain → UPDATE DB → rm temp

### ffmpeg 命令

```bash
ffmpeg -i /tmp/wemusic_gain_BVxxx.mp4 \
  -af ebur128=video=0 \
  -f null -
```

取 stderr 中 `Integrated loudness:` 后的 `I:` 值（单位 LUFS）。

### Gain 计算

```js
const TARGET = -18; // LUFS（消费端标准）
function calcGain(lufs) {
  return clamp(10 ** ((TARGET - lufs) / 20), 0.05, 10.0);
}
// 客户端安全阀：≤ 8.0
// 例：-16.5 LUFS → gain = 10^(1.5/20) ≈ 1.19
```

### 串行队列

```js
let _gainQueue = Promise.resolve();
function enqueueAnalysis(bvid, cid, streamUrl) {
  _gainQueue = _gainQueue.then(() => runAnalysis(bvid, cid, streamUrl));
}
```

### 临时文件

- 路径：`/tmp/wemusic_gain_${bvid}_${cid}_${Date.now()}.mp4`
- 完成后立即 `fs.unlinkSync()`
- 启动时清理 `/tmp/wemusic_gain_*` 残留
- 串行队列保证同时最多 1 个

### 前端数据获取

`GET /api/play/gain?bvid=xxx`（fire-and-forget，不阻塞播放）

```json
{"gain": 0.47, "status": "complete"}
{"gain": null, "status": "pending"}
{"gain": null, "status": null}
```

### Gain 上下限

防止极端值导致失真或静音：

| 层级 | 参数 | 值 | 说明 |
|------|------|-----|------|
| 服务器 | clamp | 0.05 ~ 10.0 | 源级 |
| 客户端 | 安全阀 | ≤ 8.0 | 输出级 final，防止炸耳 |

### 归一化开关

- 存储：localStorage `wemusic_volume_normalize`（"1" / "0"），默认 "1"
- 位置：设置面板 > toggle-switch
- 关闭时 `_applyNormVol` 跳过归一化增益 → GainNode gain = 1.0

---

## 参数一览

| 参数 | 值 | 说明 |
|------|-----|------|
| 目标 LUFS | -18 | 消费端标准（Apple/YouTube 区间） |
| 源级 clamp | 0.05 ~ 10.0 | 服务器端 |
| 输出安全阀 | ≤ 8.0 | 客户端 final |
| 归一化开关 | localStorage `wemusic_volume_normalize` | 默认 1（开启） |
| 短音频跳过 | < 10s | 不分析 |

---

## 改动汇总

| 文件 | 行数 | 内容 |
|------|------|------|
| `server/db.js` | +12 | gain_cache 表 + title 迁移 |
| `server/routes/play.js` | +110 | 分析队列、stream 触发、gains API |
| `server/services/bilibili.js` | +10 | getVideoTitle() |
| `src/player.js` | +60 | AudioContext、GainNode、_fetchGain、tooltip |
| `src/settings.js` | +8 | 开关逻辑 + 事件通知 |
| `public/index.html` | +10 | toggle HTML |

---

## 踩坑记录

1. **ffprobe 不可用** → 改用 `@ffmpeg-installer/ffmpeg` npm 包零配置部署
2. **ffprobe `amovie` 滤镜不兼容流式 MP4** → 改用 ffmpeg `ebur128` 音频滤镜
3. **`meter=0` 参数在 ffmpeg 4.4 无效** → 移除该参数（范围 9-18）
4. **正则匹配到逐帧 I: -70.0 静音值** → 改为匹配 Summary 中的 `Integrated loudness`
5. **ebur128 完成后 exit code ≠ 0** → 不把非零码当错误，正常解析 stderr
6. **-23 LUFS 太保守，gain 几乎全是 0.3** → 改为 -18 + 放宽 clamp + 安全阀
7. **音量滑块不可见（2026-07-10）** → `.player`、`.player-left`、`.controls` 三层 `overflow: hidden` 裁剪了 `#volBar`（absolute 定位溢出到 `.player` 上方）。移除三处 `overflow: hidden`

---

## 调试命令

```bash
# 查看分析结果
curl http://localhost:5174/api/play/gains

# 实时分析日志
tail -f /tmp/wemusic.log | grep '\[gain\]'

# 清空缓存重来
sqlite3 data/wemusic.sqlite "DELETE FROM gain_cache"
```

---

## 当前状态（2026-07-12）

### 已验证通过

归一化系统全部功能正常，分析结果示例：

| bvid | title | LUFS | gain |
|------|-------|------|------|
| BV1QqzpYWEMu | 【1080P修复·MV】华语乐坛经典 | -16.9 | 0.50x |
| BV14s4y1Q7Dr | 周杰伦-晴天 MV | -12.9 | 0.31x |
| BV1nNRiYmEem | （极轻量音频） | -33.6 | 3.00x |

### 关键约束

- **受 scope 限制**：归一化通过 GainNode 控制 bgAudio，故仅在默认模式（`mode === 'bg'`，占绝大多数使用场景）下生效。展开视频看画面时（`mode === 'iframe'`），声音由 B 站 iframe 独立输出，GainNode 无法介入
- **开关默认开启**：用户需主动去设置面板关闭（`wemusic_volume_normalize`）
- **首次播放无归一化**：分析在后台异步进行，第二次播放才生效
- **分析零配置**：`@ffmpeg-installer/ffmpeg` 作为 npm 依赖，部署即用
