## AI 协作规范

### 项目全局视图

在开始任何代码修改前，**先读取 `.project-graph.json`** 了解项目结构。

该文件由 `npm run codegraph` 自动生成（每次 build/deploy 前执行），包含模块导出符号、import 依赖关系、API 路由端点。比 grep 更快更准确。

### 需求实现流程

收到任何功能需求或代码修改时，**禁止直接跳到编码**。必须先完成：

1. **设计讨论**（至少覆盖 3 个维度）：
   - 数据流：从哪来、流向哪、空值怎么处理
   - 状态机：涉及哪些状态、如何流转、会不会跟已有状态冲突
   - 影响面：改这里会打破哪个模块的现有行为
   - 失败模式：每种失败用户看到什么
   - 取舍：有没有更简单的方案，为什么没选

2. **方案对比**：给出至少 2 个可选方向，说明各自优缺点，推荐其中一种并给理由。

3. **修改后验证**：运行 `npm run test`（AST 依赖检查 + vitest）。涉及 UI 变更时注意 JS 控制台错误。

### 长期约束

- **禁止承诺「以后我会…」**——这类承诺仅当前会话有效。
- 需要跨会话持久化的约束，**必须写入 `.codebuddy/rules/`**，不要依赖 Memory。
